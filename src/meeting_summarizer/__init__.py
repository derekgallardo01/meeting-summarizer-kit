"""Meeting summarizer kit - transcripts in, structured recaps out.

Default LLM backend is a deterministic stub so the kit runs anywhere
without keys. Set MEETING_SUMMARIZER_LLM=claude (with ANTHROPIC_API_KEY)
to route through Claude.
"""
__version__ = "1.0.0"
